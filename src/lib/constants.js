// Single-event app — there is no event switcher anywhere in the UI, so the
// event id is hardcoded here rather than threaded through every query.
export const CURRENT_EVENT_ID = '38dd7b41-90c8-4052-8781-1ccc73ca020a';
